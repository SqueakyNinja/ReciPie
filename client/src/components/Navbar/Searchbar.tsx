import styles from "./Searchbar.module.scss";

const Searchbar = () => (
    <div className={styles.Searchbar}>
    <input type="text" placeholder="Search ingredients" /></div>
)

export default Searchbar;